server:
  port: 8082

spring:
  application:
    name: user-service

  data:
    mongodb:
      uri: ${MONGODB_URI}
      database: user-service-db

management:
  endpoints:
    web:
      exposure:
        include: health,info

eureka:
  client:
    enabled: false
